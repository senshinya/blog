# 三语言（中／英／日）i18n 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给博客加上中、英、日三语言支持：中文 URL 保持不变，英日走 `/en/*` `/ja/*`，首次访问按浏览器语言落地并固化偏好，左栏加语言切换器。

**Architecture:** 内容按语言分目录，每语言一个 Nuxt Content collection，三语路径对等（44 篇文章的 slug 已是 ASCII 英文，故不需要 translationKey）。路由用 `@nuxtjs/i18n` 的 `prefix_except_default` + `defaultLocale: 'zh'`。一个构建期 Nuxt 模块扫描内容与游记，产出「路径 → 有哪几种语言」的清单，编译进 bundle，供导航中间件、切换器灰显、hreflang 与预渲染路由四方消费。

**Tech Stack:** Nuxt 4.5.2、@nuxt/content 3.16.0、@nuxtjs/i18n 10.6.0、vue-i18n、SCSS、`node:test`

**Spec:** `docs/superpowers/specs/2026-09-09-i18n-design.md`

## Global Constraints

- Node `^22.19 || ^24.11 || >=26`，pnpm `>=10`。
- 新依赖必须走 pnpm catalog：在 `pnpm-workspace.yaml` 的 `catalogs.<组>` 里登记版本，`package.json` 写 `catalog:<组>`，不写裸版本号。
- 中文 URL 必须逐字节不变。任何改动若使 `/daily/anti-chronic-gastritis` 这类路径变化，即为失败。
- 不做内容级回落：某页缺译文时不以其他语言内容顶替。
- 评论线程不合并，三语各自成线程。
- 不翻译源码注释，注释保持中文。
- 测试用 `node:test` + `node:assert/strict`。Vitest 不是本项目依赖，不要引入。
- 缩进用 Tab，与仓库现有代码一致。
- SCSS 中 `_variable.scss` 已由 `vite.css.preprocessorOptions.scss.additionalData` 自动注入，无需手动 `@use`。
- 每个任务结束时 `pnpm lint` 必须通过。

## 基线

开工前确认：

```bash
node --test "app/**/*.test.ts"   # 应为 28 pass / 0 fail
git rev-parse --abbrev-ref HEAD  # 应为 feat/i18n
```

---

## 阶段 A：地基

### Task 1: 探针——确认 collection 前缀与 afterParse hook 的交互

Spec 的「待验证前提」。`source: { include: 'zh/**', prefix: '' }` 剥掉语言段后，`nuxt.config.ts:293` 的 `content:file:afterParse` 拿到的 `path` 究竟长什么样，决定后续所有任务是否需要改这个 hook。本任务的交付物是一个答案，探针代码用完即删。

**Files:**
- Modify（临时）: `content.config.ts`
- Modify（临时）: `nuxt.config.ts:292-301`
- Modify: `docs/superpowers/specs/2026-09-09-i18n-design.md`（记录结论）

**Interfaces:**
- Consumes: 无
- Produces: 一个结论，Task 2 依据它决定是否修改 hook

- [ ] **Step 1: 临时把内容挪进 zh/ 子目录**

```bash
mkdir -p content/zh
git mv content/posts content/zh/posts
git mv content/friends.md content/zh/friends.md
```

- [ ] **Step 2: 临时改成单个带前缀剥离的 collection**

`content.config.ts` 里把 `collections` 换成：

```ts
export const collections = {
	content: defineCollection({
		source: { include: 'zh/**', prefix: '' },
		type: 'page',
		schema: articleSchema.extend({
			sitemap: defineSitemapSchema({
				name: 'content',
				onUrl: (url, entry) => {
					const lastmod = (entry.published || entry.date) as string | undefined
					if (lastmod)
						url.lastmod = new Date(lastmod).toLocaleDateString('sv')
				},
				z,
			}),
		}),
	}),
}
```

- [ ] **Step 3: 在 hook 里打印实际 path**

`nuxt.config.ts` 的 `content:file:afterParse` 首行插入：

```ts
'content:file:afterParse': (ctx) => {
	const { permalink, path } = ctx.content as Record<string, string | undefined>
	console.info('[PROBE] path =', JSON.stringify(path))
	if (permalink)
		ctx.content.path = permalink
	else if (blogConfig.article.hidePostPrefix && path?.startsWith('/posts/'))
		ctx.content.path = path.slice('/posts'.length)
},
```

- [ ] **Step 4: 跑构建，收集探针输出**

Run: `pnpm build 2>&1 | grep '\[PROBE\]' | sort -u | head -20`

记下结果属于哪种情况：
- **情况 A**：打印 `/posts/daily/anti-chronic-gastritis` —— 前缀已被剥掉，hook 无需改动。
- **情况 B**：打印 `/zh/posts/daily/anti-chronic-gastritis` —— 语言段还在，hook 需要先剥语言段。

- [ ] **Step 5: 移除探针，把结论写进 spec**

删掉 `console.info('[PROBE]', ...)` 那一行。在 spec 的「待验证前提」一节末尾追加一段，写明实测结论与日期。若为情况 B，同时把该节改写为「hook 需在剥 `/posts` 之前先剥语言段」，供 Task 2 执行。

- [ ] **Step 6: 验证站点行为未变**

Run: `pnpm build && node --test "app/**/*.test.ts"`

Expected: 构建成功；28 tests pass。抽查 `.output/public/daily/anti-chronic-gastritis/index.html` 存在（情况 A 下路径不变即为正确）。

```bash
ls .output/public/daily/anti-chronic-gastritis/index.html
```

- [ ] **Step 7: 提交**

```bash
git add content content.config.ts docs/superpowers/specs/2026-09-09-i18n-design.md
git commit -m "refactor(content): 内容迁入 content/zh，确认 collection 前缀剥离行为"
```

---

### Task 2: 拆成三个按语言的 collection

**Files:**
- Modify: `content.config.ts`
- Create: `content/en/.gitkeep`
- Create: `content/ja/.gitkeep`
- Modify（仅当 Task 1 判定为情况 B）: `nuxt.config.ts:292-301`

**Interfaces:**
- Consumes: Task 1 的结论
- Produces: collection 名 `content_zh` / `content_en` / `content_ja`；类型 `Locale = 'zh' | 'en' | 'ja'`

- [ ] **Step 1: 建立三语言的 collection**

`content.config.ts` 末尾替换 `collections`：

```ts
export const LOCALES = ['zh', 'en', 'ja'] as const
export type Locale = typeof LOCALES[number]

function makeCollection(locale: Locale) {
	return defineCollection({
		source: { include: `${locale}/**`, prefix: '' },
		type: 'page',
		schema: articleSchema.extend({
			sitemap: defineSitemapSchema({
				name: `content_${locale}`,
				onUrl: (url, entry) => {
					const lastmod = (entry.published || entry.date) as string | undefined
					if (lastmod)
						url.lastmod = new Date(lastmod).toLocaleDateString('sv')
				},
				z,
			}),
		}),
	})
}

export const collections = {
	content_zh: makeCollection('zh'),
	content_en: makeCollection('en'),
	content_ja: makeCollection('ja'),
}
```

- [ ] **Step 2: 建空目录占位**

```bash
mkdir -p content/en content/ja
touch content/en/.gitkeep content/ja/.gitkeep
```

- [ ] **Step 3: 把 8 处 queryCollection 改成 content_zh**

此时还没有 i18n，先全部硬编码为 `content_zh`，Task 6 之后再改成按 locale 取。逐个替换：

| 文件 | 原调用 |
|---|---|
| `app/composables/useArticle.ts` | `queryCollection('content')` |
| `app/components/post/PostSurround.vue` | `queryCollectionItemSurroundings('content', ...)` |
| `app/components/popover/Search.vue` | `queryCollectionSearchSections('content', ...)` |
| `server/api/stats.get.ts` | `queryCollection(event, 'content')` |
| `app/pages/index.vue` | `queryCollection('content')` |
| `app/pages/[...slug].vue` | `queryCollection('content')` |
| `app/pages/friends.vue` | `queryCollection('content')` |
| `server/routes/atom.xml.get.ts` | `queryCollection(event, 'content')` |

```bash
grep -rn "queryCollection\w*(\(event, \)\?'content'" app server
```

每处把 `'content'` 换成 `'content_zh'`。

- [ ] **Step 4: 若 Task 1 判定情况 B，修 hook**

仅在情况 B 下执行。`nuxt.config.ts` 的 hook 改为：

```ts
'content:file:afterParse': (ctx) => {
	const { permalink, path } = ctx.content as Record<string, string | undefined>
	// 先剥语言段（/zh /en /ja），再走原有逻辑
	const stripped = path?.replace(/^\/(?:zh|en|ja)(?=\/|$)/, '')
	if (permalink)
		ctx.content.path = permalink
	else if (blogConfig.article.hidePostPrefix && stripped?.startsWith('/posts/'))
		ctx.content.path = stripped.slice('/posts'.length)
	else if (stripped)
		ctx.content.path = stripped
},
```

- [ ] **Step 5: 构建并验证 URL 未变**

Run:

```bash
pnpm build
ls .output/public/daily/anti-chronic-gastritis/index.html
ls .output/public/fiddling/tech-about-gfw/index.html 2>/dev/null || ls .output/public/tech-about-gfw/index.html
node --test "app/**/*.test.ts"
```

Expected: 两个文章页产物存在于与改动前相同的路径；28 tests pass。

- [ ] **Step 6: 提交**

```bash
git add -A
git commit -m "refactor(content): 拆成 content_zh/en/ja 三个 collection"
```

---

### Task 3: 装 @nuxtjs/i18n 并配好路由骨架

**Files:**
- Modify: `pnpm-workspace.yaml`（catalogs.content）
- Modify: `package.json`
- Modify: `nuxt.config.ts`（modules、新增 i18n 段）
- Modify: `blog.config.ts`（新增 locales 定义）

**Interfaces:**
- Consumes: Task 2 的 `Locale` 类型
- Produces: `blogConfig.locales`（数组，每项 `{ code, language, name, file }`）；可用的 `useI18n()` / `useLocalePath()` / `useSwitchLocalePath()`

- [ ] **Step 1: 登记依赖**

`pnpm-workspace.yaml` 的 `catalogs.content` 下按字母序插入：

```yaml
    '@nuxtjs/i18n': ^10.6.0
```

`package.json` 的 `dependencies` 里按字母序插入（在 `@nuxt/image` 与 `@nuxtjs/color-mode` 之间）：

```json
    "@nuxtjs/i18n": "catalog:content",
```

- [ ] **Step 2: 安装**

Run: `pnpm install`
Expected: 安装成功，`node_modules/@nuxtjs/i18n` 存在。

- [ ] **Step 3: 在 blog.config.ts 定义 locales**

`basicConfig` 中 `language: 'zh-CN'` 保留（`@nuxtjs/seo` 的 `site.defaultLocale` 仍用它）。在 `blogConfig` 对象里新增：

```ts
	/**
	 * 三语言定义。name 是语言切换器上的按钮标签，用各语言自称，
	 * 任何语言环境下都是这个形态，故不进词条表。
	 */
	locales: [
		{ code: 'zh', language: 'zh-CN', name: '中', file: 'zh.ts' },
		{ code: 'en', language: 'en-US', name: 'En', file: 'en.ts' },
		{ code: 'ja', language: 'ja-JP', name: 'あ', file: 'ja.ts' },
	],
```

- [ ] **Step 4: 接进 nuxt.config**

`modules` 数组里加 `'@nuxtjs/i18n'`。在 `nuxt.config.ts` 顶层（与 `site`、`robots` 同级，按 @keep-sorted 的字母序放在 `icon` 之后、`llms` 之前）加：

```ts
	i18n: {
		locales: blogConfig.locales,
		defaultLocale: 'zh',
		strategy: 'prefix_except_default',
		// 关掉自带检测：它只在入口判一次且不认识清单，与 01.locale-preference
		// 中间件是竞争关系，同时开会双重跳转。
		detectBrowserLanguage: false,
		lazy: true,
		langDir: 'locales/',
		bundle: { optimizeTranslationDirective: false },
	},
```

- [ ] **Step 5: 建三个占位词条文件**

```bash
mkdir -p i18n/locales
for l in zh en ja; do printf 'export default {}\n' > "i18n/locales/$l.ts"; done
```

- [ ] **Step 6: 构建并验证中文 URL 未变、前缀路由已生成**

Run:

```bash
pnpm build
ls .output/public/daily/anti-chronic-gastritis/index.html   # 中文路径必须仍在
ls .output/public/en/index.html .output/public/ja/index.html
```

Expected: 中文文章页路径不变；`/en/` `/ja/` 首页产物已生成（内容此时仍是中文，正常）。

- [ ] **Step 7: 提交**

```bash
git add -A
git commit -m "feat(i18n): 接入 @nuxtjs/i18n，配置三语言 prefix_except_default 路由"
```

---

## 阶段 B：清单与语言决策

### Task 4: 构建期清单模块

**Files:**
- Create: `modules/i18n-manifest/index.ts`
- Create: `modules/i18n-manifest/scan.ts`
- Create: `modules/i18n-manifest/scan.test.ts`
- Modify: `package.json`（新增 test script）

**Interfaces:**
- Consumes: `content.config.ts` 的 `LOCALES` / `Locale`
- Produces:
  - `scanLocaleTrees(opts: { contentDir: string, travelsDir: string, locales: readonly string[], isDev: boolean }): Manifest`
  - `toPrerenderRoutes(manifest: Manifest, defaultLocale: string): string[]`
  - `type Manifest = Record<string, string[]>`（key 为不带语言前缀的路由路径，value 为该路径存在的语言码，已排序）
  - 虚拟模块 `#build/i18n-manifest`，导出 `manifest: Manifest` 与 `prerenderRoutes: string[]`
  - `pnpm test` script

`app/utils/locale.ts`（Task 5）会各自再声明一份结构相同的 `Manifest` 类型。这是有意的：`app/` 下的运行时代码不应该 import `modules/` 里的构建期代码，两边靠结构相同保持一致。

- [ ] **Step 1: 加 test script**

`package.json` 的 `scripts` 里，在 `"preview"` 之后插入：

```json
    "test": "node --test \"app/**/*.test.ts\" \"modules/**/*.test.ts\" \"i18n/**/*.test.ts\"",
```

- [ ] **Step 2: 写失败的测试**

Create `modules/i18n-manifest/scan.test.ts`：

```ts
import assert from 'node:assert/strict'
import { mkdtempSync, mkdirSync, writeFileSync } from 'node:fs'
import { tmpdir } from 'node:os'
import { join } from 'node:path'
// eslint-disable-next-line test/no-import-node-test -- Vitest is not a project dependency; use Node's built-in runner.
import test from 'node:test'

function fixture() {
	const root = mkdtempSync(join(tmpdir(), 'i18n-scan-'))
	const content = join(root, 'content')
	const travels = join(root, 'travels')
	for (const l of ['zh', 'en', 'ja']) {
		mkdirSync(join(content, l, 'posts', 'daily'), { recursive: true })
		mkdirSync(join(travels, l), { recursive: true })
	}
	// 中英有译文，日文没有
	writeFileSync(join(content, 'zh/posts/daily/gastritis.md'), '---\ntitle: 胃炎\n---\n')
	writeFileSync(join(content, 'en/posts/daily/gastritis.md'), '---\ntitle: Gastritis\n---\n')
	// 只有中文
	writeFileSync(join(content, 'zh/friends.md'), '---\ntitle: 友链\n---\n')
	// 游记：中文与日文
	writeFileSync(join(travels, 'zh/kansai-202504.yaml'), 'slug: kansai-202504\ntitle: 近畿地方\n')
	writeFileSync(join(travels, 'ja/kansai-202504.yaml'), 'slug: kansai-202504\ntitle: 近畿地方\n')
	// 草稿：生产环境应被排除
	writeFileSync(join(travels, 'zh/secret-202601.yaml'), 'slug: secret-202601\ndraft: true\n')
	return { content, travels }
}

test('maps each route path to the locales that have it', async () => {
	const { scanLocaleTrees } = await import('./scan.ts')
	const { content, travels } = fixture()
	const manifest = scanLocaleTrees({
		contentDir: content,
		travelsDir: travels,
		locales: ['zh', 'en', 'ja'],
		isDev: false,
	})
	assert.deepEqual(manifest['/daily/gastritis'], ['en', 'zh'])
	assert.deepEqual(manifest['/friends'], ['zh'])
	assert.deepEqual(manifest['/travels/kansai-202504'], ['ja', 'zh'])
})

test('strips the /posts prefix from article paths', async () => {
	const { scanLocaleTrees } = await import('./scan.ts')
	const { content, travels } = fixture()
	const manifest = scanLocaleTrees({ contentDir: content, travelsDir: travels, locales: ['zh', 'en', 'ja'], isDev: false })
	assert.equal(Object.keys(manifest).some(k => k.startsWith('/posts/')), false)
})

test('excludes draft travels outside development', async () => {
	const { scanLocaleTrees } = await import('./scan.ts')
	const { content, travels } = fixture()
	const prod = scanLocaleTrees({ contentDir: content, travelsDir: travels, locales: ['zh', 'en', 'ja'], isDev: false })
	assert.equal(prod['/travels/secret-202601'], undefined)
	const dev = scanLocaleTrees({ contentDir: content, travelsDir: travels, locales: ['zh', 'en', 'ja'], isDev: true })
	assert.deepEqual(dev['/travels/secret-202601'], ['zh'])
})
```

- [ ] **Step 3: 跑测试确认失败**

Run: `node --test modules/i18n-manifest/scan.test.ts`
Expected: FAIL，报找不到 `./scan.ts`

- [ ] **Step 4: 实现扫描器**

Create `modules/i18n-manifest/scan.ts`：

```ts
import { readdirSync, readFileSync, statSync } from 'node:fs'
import { join, relative, sep } from 'node:path'
import { isTravelDraftSource } from '../../app/travels/draft.ts'

export type Manifest = Record<string, string[]>

interface ScanOptions {
	contentDir: string
	travelsDir: string
	locales: readonly string[]
	isDev: boolean
}

function walk(dir: string): string[] {
	let out: string[] = []
	let entries: string[]
	try {
		entries = readdirSync(dir)
	}
	catch {
		return out
	}
	for (const name of entries) {
		const full = join(dir, name)
		if (statSync(full).isDirectory())
			out = out.concat(walk(full))
		else
			out.push(full)
	}
	return out
}

/** content/<locale>/posts/daily/foo.md → /daily/foo；content/<locale>/friends.md → /friends */
function contentRoutePath(localeRoot: string, file: string) {
	const rel = relative(localeRoot, file).split(sep).join('/')
	const noExt = rel.replace(/\.md$/, '')
	const withSlash = `/${noExt}`
	return withSlash.startsWith('/posts/') ? withSlash.slice('/posts'.length) : withSlash
}

function add(manifest: Manifest, path: string, locale: string) {
	const list = manifest[path] ??= []
	if (!list.includes(locale))
		list.push(locale)
}

export function scanLocaleTrees({ contentDir, travelsDir, locales, isDev }: ScanOptions): Manifest {
	const manifest: Manifest = {}

	for (const locale of locales) {
		const root = join(contentDir, locale)
		for (const file of walk(root)) {
			if (!file.endsWith('.md'))
				continue
			add(manifest, contentRoutePath(root, file), locale)
		}
	}

	for (const locale of locales) {
		const root = join(travelsDir, locale)
		for (const file of walk(root)) {
			if (!file.endsWith('.yaml'))
				continue
			if (!isDev && isTravelDraftSource(readFileSync(file, 'utf8')))
				continue
			const slug = relative(root, file).split(sep).join('/').replace(/\.yaml$/, '')
			add(manifest, `/travels/${slug}`, locale)
		}
	}

	for (const path of Object.keys(manifest))
		manifest[path]!.sort()

	return manifest
}

/** 把清单摊平成预渲染路由：默认语言不带前缀，其余带前缀。 */
export function toPrerenderRoutes(manifest: Manifest, defaultLocale: string): string[] {
	const routes: string[] = []
	for (const [path, localeList] of Object.entries(manifest)) {
		for (const locale of localeList)
			routes.push(locale === defaultLocale ? path : `/${locale}${path}`)
	}
	return routes.sort()
}
```

- [ ] **Step 5: 跑测试确认通过**

Run: `node --test modules/i18n-manifest/scan.test.ts`
Expected: 3 tests pass

- [ ] **Step 6: 写模块本体**

Create `modules/i18n-manifest/index.ts`：

```ts
import { addTemplate, addTypeTemplate, defineNuxtModule } from '@nuxt/kit'
import { resolve } from 'node:path'
import { scanLocaleTrees, toPrerenderRoutes } from './scan.ts'

export interface ModuleOptions {
	locales: readonly string[]
	defaultLocale: string
}

export default defineNuxtModule<ModuleOptions>({
	meta: { name: 'i18n-manifest', configKey: 'i18nManifest' },
	setup(options, nuxt) {
		const manifest = scanLocaleTrees({
			contentDir: resolve(nuxt.options.rootDir, 'content'),
			travelsDir: resolve(nuxt.options.rootDir, 'app/travels'),
			locales: options.locales,
			isDev: nuxt.options.dev,
		})

		const prerenderRoutes = toPrerenderRoutes(manifest, options.defaultLocale)

		addTemplate({
			filename: 'i18n-manifest.mjs',
			getContents: () => [
				`export const manifest = ${JSON.stringify(manifest, null, 2)}`,
				`export const prerenderRoutes = ${JSON.stringify(prerenderRoutes, null, 2)}`,
			].join('\n'),
		})

		addTypeTemplate({
			filename: 'i18n-manifest.d.ts',
			getContents: () => [
				`declare module '#build/i18n-manifest' {`,
				`	export const manifest: Record<string, string[]>`,
				`	export const prerenderRoutes: string[]`,
				`}`,
			].join('\n'),
		})

		nuxt.options.nitro.prerender ??= {}
		nuxt.options.nitro.prerender.routes = [
			...(nuxt.options.nitro.prerender.routes ?? []),
			...prerenderRoutes,
		]
	},
})
```

- [ ] **Step 7: 接进 nuxt.config**

`modules` 数组里加（放在 `'@nuxtjs/i18n'` 之后）：

```ts
		['~~/modules/i18n-manifest', { locales: ['zh', 'en', 'ja'], defaultLocale: 'zh' }],
```

- [ ] **Step 8: 构建并验证清单产出**

Run:

```bash
pnpm build
cat .nuxt/i18n-manifest.mjs | head -20
```

Expected: 能看到 `/daily/anti-chronic-gastritis: ["zh"]` 一类条目；`/travels/kansai-202504` 与 `/travels/korea-202510` 各为 `["zh"]`。

- [ ] **Step 9: 全量测试 + lint + 提交**

```bash
pnpm test && pnpm lint
git add -A
git commit -m "feat(i18n): 构建期清单模块，产出路径与语言的映射"
```

---

### Task 5: decideLocale 纯函数

清单模块之外最值得测的一块。所有分支逻辑集中在这里，中间件只负责接线。

**Files:**
- Create: `app/utils/locale.ts`
- Create: `app/utils/locale.test.ts`

**Interfaces:**
- Consumes: `Manifest` 类型
- Produces:
  - `stripLocale(path: string, locales: readonly string[], defaultLocale: string): { locale: string, basePath: string }`
  - `resolvePreferred(stored: string | undefined, browser: readonly string[], locales: readonly string[]): string | undefined` —— 解析偏好语言，与是否跳转无关
  - `decideLocale(input: DecideInput): string | undefined` —— 返回应跳转到的目标路径；`undefined` 表示不跳转
  - `interface DecideInput { path: string, stored?: string, browser: readonly string[], manifest: Manifest, locales: readonly string[], defaultLocale: string }`

**注意**：`stripLocale` 的第三个参数是显式的默认语言，不从 `locales[0]` 推断 ——
`blogConfig.locales` 的顺序同时是语言切换器的按钮顺序，是会被重排的东西。

**注意**：`resolvePreferred` 必须独立于 `decideLocale` 导出。中间件要在「不跳转」的情况下
也把偏好固化进 cookie（spec 要求缺译文时停在当前页但 cookie 已写入），
若只从 `decideLocale` 的返回值反推目标语言，缺译文时就永远写不进去。

- [ ] **Step 1: 写失败的测试**

Create `app/utils/locale.test.ts`：

```ts
import assert from 'node:assert/strict'
// eslint-disable-next-line test/no-import-node-test -- Vitest is not a project dependency; use Node's built-in runner.
import test from 'node:test'

const LOCALES = ['zh', 'en', 'ja'] as const
const MANIFEST = {
	'/daily/gastritis': ['en', 'zh'],
	'/friends': ['zh'],
}

function input(over: Record<string, unknown> = {}) {
	return {
		path: '/daily/gastritis',
		stored: undefined,
		browser: [] as readonly string[],
		manifest: MANIFEST,
		locales: LOCALES,
		defaultLocale: 'zh',
		...over,
	} as never
}

test('splits the locale prefix off a path', async () => {
	const { stripLocale } = await import('./locale.ts')
	assert.deepEqual(stripLocale('/en/daily/gastritis', LOCALES, 'zh'), { locale: 'en', basePath: '/daily/gastritis' })
	assert.deepEqual(stripLocale('/daily/gastritis', LOCALES, 'zh'), { locale: 'zh', basePath: '/daily/gastritis' })
	assert.deepEqual(stripLocale('/ja', LOCALES, 'zh'), { locale: 'ja', basePath: '/' })
	assert.deepEqual(stripLocale('/', LOCALES, 'zh'), { locale: 'zh', basePath: '/' })
})

test('takes the default locale from the argument, not from the array order', async () => {
	const { stripLocale } = await import('./locale.ts')
	// locales 数组的顺序是切换器按钮顺序，不能用来推默认语言
	assert.equal(stripLocale('/daily/gastritis', ['ja', 'en', 'zh'], 'zh').locale, 'zh')
})

test('resolvePreferred prefers the stored value, then the browser list', async () => {
	const { resolvePreferred } = await import('./locale.ts')
	assert.equal(resolvePreferred('en', ['ja'], LOCALES), 'en')
	assert.equal(resolvePreferred(undefined, ['ja-JP', 'en'], LOCALES), 'ja')
	assert.equal(resolvePreferred(undefined, ['de-DE'], LOCALES), undefined)
	// 存了一个本站不支持的值，应当忽略它并回退到浏览器语言
	assert.equal(resolvePreferred('de', ['en-US'], LOCALES), 'en')
})

test('stored preference wins over the browser languages', async () => {
	const { decideLocale } = await import('./locale.ts')
	assert.equal(decideLocale(input({ stored: 'en', browser: ['ja'] })), '/en/daily/gastritis')
})

test('falls back to browser languages when nothing is stored', async () => {
	const { decideLocale } = await import('./locale.ts')
	assert.equal(decideLocale(input({ browser: ['en-US', 'en'] })), '/en/daily/gastritis')
})

test('stays put when the preferred locale already matches', async () => {
	const { decideLocale } = await import('./locale.ts')
	assert.equal(decideLocale(input({ path: '/en/daily/gastritis', stored: 'en' })), undefined)
	assert.equal(decideLocale(input({ stored: 'zh' })), undefined)
})

test('stays put when the page has no translation in the preferred locale', async () => {
	const { decideLocale } = await import('./locale.ts')
	assert.equal(decideLocale(input({ stored: 'ja' })), undefined)
	assert.equal(decideLocale(input({ path: '/friends', stored: 'en' })), undefined)
})

test('treats paths outside the manifest as available in every locale', async () => {
	const { decideLocale } = await import('./locale.ts')
	assert.equal(decideLocale(input({ path: '/archive', stored: 'ja' })), '/ja/archive')
})

test('ignores browser languages the site does not support', async () => {
	const { decideLocale } = await import('./locale.ts')
	assert.equal(decideLocale(input({ browser: ['de-DE', 'fr'] })), undefined)
})

test('maps regional browser tags onto the base locale', async () => {
	const { decideLocale } = await import('./locale.ts')
	assert.equal(decideLocale(input({ path: '/archive', browser: ['ja-JP'] })), '/ja/archive')
})

test('builds default-locale targets without a prefix', async () => {
	const { decideLocale } = await import('./locale.ts')
	assert.equal(decideLocale(input({ path: '/en/daily/gastritis', stored: 'zh' })), '/daily/gastritis')
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test app/utils/locale.test.ts`
Expected: FAIL，报找不到 `./locale.ts`

- [ ] **Step 3: 实现**

Create `app/utils/locale.ts`：

```ts
export type Manifest = Record<string, string[]>

export interface DecideInput {
	/** 当前路由路径，含可能的语言前缀 */
	path: string
	/** cookie 里记住的偏好，没有则 undefined */
	stored?: string
	/** navigator.languages，按优先级排列 */
	browser: readonly string[]
	manifest: Manifest
	locales: readonly string[]
	defaultLocale: string
}

/**
 * 把路径拆成语言码与不带前缀的基准路径。无前缀即默认语言。
 *
 * defaultLocale 显式传入，不从 locales[0] 推断 —— 那个数组的顺序
 * 同时是语言切换器的按钮顺序，会被重排。
 */
export function stripLocale(path: string, locales: readonly string[], defaultLocale: string) {
	const match = /^\/([^/]+)(?=\/|$)/.exec(path)
	const head = match?.[1]
	if (head && locales.includes(head)) {
		const rest = path.slice(head.length + 1)
		return { locale: head, basePath: rest || '/' }
	}
	return { locale: defaultLocale, basePath: path }
}

/**
 * 解析偏好语言：cookie 优先，其次浏览器语言列表，允许 ja-JP → ja。
 *
 * 与「是否跳转」解耦，独立导出 —— 中间件在缺译文而不跳转时也要固化这个值。
 */
export function resolvePreferred(
	stored: string | undefined,
	browser: readonly string[],
	locales: readonly string[],
) {
	if (stored && locales.includes(stored))
		return stored
	for (const tag of browser) {
		const base = tag.toLowerCase().split('-')[0]!
		const hit = locales.find(l => l === base)
		if (hit)
			return hit
	}
	return undefined
}

function buildPath(basePath: string, locale: string, defaultLocale: string) {
	if (locale === defaultLocale)
		return basePath
	return basePath === '/' ? `/${locale}` : `/${locale}${basePath}`
}

/**
 * 返回应当跳转到的路径，undefined 表示留在原地。
 *
 * 留在原地的三种情形：没有可用偏好、偏好与当前语言一致、当前页没有偏好语言的译文。
 * 清单里查不到的路径视为所有语言都有 —— 那是应用页面（/archive 等），
 * i18n 会为每个语言生成。
 */
export function decideLocale({ path, stored, browser, manifest, locales, defaultLocale }: DecideInput) {
	const preferred = resolvePreferred(stored, browser, locales)
	if (!preferred)
		return undefined

	const { locale: current, basePath } = stripLocale(path, locales, defaultLocale)
	if (preferred === current)
		return undefined

	const available = manifest[basePath]
	if (available && !available.includes(preferred))
		return undefined

	return buildPath(basePath, preferred, defaultLocale)
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `node --test app/utils/locale.test.ts`
Expected: 12 tests pass

- [ ] **Step 5: 提交**

```bash
pnpm lint
git add app/utils/locale.ts app/utils/locale.test.ts
git commit -m "feat(i18n): decideLocale 语言决策纯函数"
```

---

### Task 6: 全局中间件接线

**Files:**
- Create: `app/middleware/01.locale-preference.global.ts`
- Create: `app/composables/useLocalePreference.ts`

**Interfaces:**
- Consumes: `decideLocale`、`stripLocale`、`#build/i18n-manifest`
- Produces: `useLocalePreference(): { stored: Ref<string | null>, persist: (code: string) => void }` —— Task 8 的切换器复用它写 cookie

- [ ] **Step 1: 偏好存储 composable**

Create `app/composables/useLocalePreference.ts`：

```ts
/**
 * 语言偏好存储。在 prefix_except_default 下「设置语言」等同于「导航」，
 * 所以这个 cookie 是纯粹的偏好记录，不参与路由，与 i18n 的职责不重叠。
 */
export function useLocalePreference() {
	const stored = useCookie<string | null>('blog_locale', {
		default: () => null,
		maxAge: 60 * 60 * 24 * 365,
		sameSite: 'lax',
		path: '/',
	})

	function persist(code: string) {
		stored.value = code
	}

	return { stored, persist }
}
```

- [ ] **Step 2: 中间件**

Create `app/middleware/01.locale-preference.global.ts`：

```ts
import { manifest } from '#build/i18n-manifest'
import blogConfig from '~~/blog.config'
import { decideLocale, resolvePreferred } from '~/utils/locale'

const LOCALES = blogConfig.locales.map(l => l.code)

/**
 * 文件名的 01. 前缀：Nuxt 按字母序执行全局中间件，需排在 @nuxtjs/i18n
 * 注入的那些之后。
 *
 * 整体以 import.meta.client 守卫，因此不参与服务端与预渲染输出 ——
 * 服务端与首次水合渲染的是同一份 HTML，本路径不产生 hydration mismatch。
 * 重定向切换的是路由而非当前路由的渲染结果，新路由整页重渲。
 */
export default defineNuxtRouteMiddleware((to) => {
	if (import.meta.server)
		return

	const { stored, persist } = useLocalePreference()
	const browser = navigator.languages ?? [navigator.language].filter(Boolean)

	/**
	 * 固化与跳转是两件事，必须分开算。
	 *
	 * 缺译文时不跳转，但偏好照样要写进 cookie（spec 要求）——
	 * 否则日语读者每次进站都重新检测，且因为落地页恰好没有日文版，
	 * 永远进不了日语版。
	 */
	const preferred = resolvePreferred(stored.value ?? undefined, browser, LOCALES)
	if (preferred && !stored.value)
		persist(preferred)

	const target = decideLocale({
		path: to.path,
		stored: stored.value ?? undefined,
		browser,
		manifest,
		locales: LOCALES,
		defaultLocale: 'zh',
	})

	if (target && target !== to.path)
		return navigateTo(target)
})
```

- [ ] **Step 3: 构建**

Run: `pnpm build`
Expected: 构建成功，无类型错误。

- [ ] **Step 4: 手动验证跳转与固化**

```bash
pnpm preview
```

浏览器开无痕窗口，把语言设为 English，访问 `http://localhost:3000/`。

Expected:
- 地址栏跳到 `/en`
- DevTools → Application → Cookies 里 `blog_locale=en`
- 刷新后不再重复跳转
- 访问 `/daily/anti-chronic-gastritis`（此页只有中文）时**不跳转**，停在中文页

- [ ] **Step 5: 提交**

```bash
pnpm test && pnpm lint
git add -A
git commit -m "feat(i18n): 语言偏好中间件，首屏检测一次即固化"
```

---

## 阶段 C：切换器

### Task 7: 抽出胶囊样式 mixin

`LangToggle` 要和 `ThemeToggle` 长得完全一致（两个胶囊上下相邻），所以先把样式抽成共用 mixin。共用样式而不共用逻辑——两者逻辑差别正是重点（见 Task 8）。

**Files:**
- Create: `app/assets/css/_mixin.scss`
- Modify: `nuxt.config.ts`（additionalData）
- Modify: `app/components/blog/ThemeToggle.vue`（样式段）

**Interfaces:**
- Produces: SCSS mixin `segmented-pill()`，供 ThemeToggle 与 LangToggle 各自 `@include`

- [ ] **Step 1: 建 mixin 文件**

Create `app/assets/css/_mixin.scss`：

```scss
/// 侧栏底部的分段胶囊控件。ThemeToggle 与 LangToggle 共用外观。
@mixin segmented-pill {
	display: flex;
	gap: 3px;
	width: fit-content;
	margin: 0 auto;
	padding: 2px;
	border: 1px solid var(--c-border);
	border-radius: 1rem;
	background-color: var(--c-bg-2);

	> button {
		padding: 4px 1rem;
		border-radius: 1rem;
		transition: all 0.1s;

		&:hover:not(:disabled) {
			background-color: var(--c-bg-soft);
			color: var(--c-text-1);
		}

		&.active {
			box-shadow: var(--box-shadow-2);
			background-color: var(--ld-bg-card);
			color: var(--c-text-1);
			cursor: auto;
		}

		&:disabled {
			opacity: 0.35;
			cursor: not-allowed;
		}
	}
}
```

- [ ] **Step 2: 注入 mixin**

`nuxt.config.ts` 的 `vite.css.preprocessorOptions.scss.additionalData` 改为：

```ts
					additionalData: '@use "@/assets/css/_variable.scss" as *; @use "@/assets/css/_mixin.scss" as *;',
```

- [ ] **Step 3: ThemeToggle 改用 mixin**

`app/components/blog/ThemeToggle.vue` 的 `<style>` 段整体替换为：

```scss
.theme-toggle {
	@include segmented-pill;
}
```

- [ ] **Step 4: 构建并肉眼确认 ThemeToggle 外观未变**

Run: `pnpm build && pnpm preview`

Expected: 侧栏底部的主题切换胶囊外观与改动前一致（浅色深色都看一眼）。

- [ ] **Step 5: 提交**

```bash
pnpm lint
git add -A
git commit -m "refactor(style): 抽出 segmented-pill mixin，ThemeToggle 改用"
```

---

### Task 8: 语言切换器组件

**Files:**
- Create: `app/components/blog/LangToggle.vue`
- Modify: `app/components/blog/BlogSidebar.vue:45-48`

**Interfaces:**
- Consumes: `useLocalePreference`、`stripLocale`、`#build/i18n-manifest`、`blogConfig.locales`
- Produces: 组件 `<BlogLangToggle />`

- [ ] **Step 1: 写组件**

Create `app/components/blog/LangToggle.vue`：

```vue
<script setup lang="ts">
import { manifest } from '#build/i18n-manifest'
import blogConfig from '~~/blog.config'
import { stripLocale } from '~/utils/locale'

const route = useRoute()
const switchLocalePath = useSwitchLocalePath()
const { persist } = useLocalePreference()

const LOCALES = blogConfig.locales.map(l => l.code)

/**
 * 当前语言由 URL 决定，服务端与客户端看到同一个值，
 * 因此不需要 ThemeToggle 那个 mounted 守卫 —— 那边的偏好存在
 * localStorage，服务端不可知，才会 hydration mismatch。
 */
const current = computed(() => stripLocale(route.path, LOCALES, 'zh').locale)

/** 清单里查不到的路径是应用页面，i18n 为每个语言都生成了 */
const available = computed(() => {
	const { basePath } = stripLocale(route.path, LOCALES, 'zh')
	return manifest[basePath] ?? LOCALES
})

function choose(code: string) {
	persist(code)
	return navigateTo(switchLocalePath(code))
}
</script>

<template>
<div class="lang-toggle">
	<button
		v-for="locale in blogConfig.locales"
		:key="locale.code"
		v-tip="available.includes(locale.code) ? locale.language : $t('lang.unavailable')"
		:aria-label="locale.language"
		:class="{ active: current === locale.code }"
		:disabled="!available.includes(locale.code)"
		:lang="locale.code"
		@click="choose(locale.code)"
	>
		{{ locale.name }}
	</button>
</div>
</template>

<style lang="scss" scoped>
.lang-toggle {
	@include segmented-pill;

	margin-bottom: 0.5rem;
}
</style>
```

- [ ] **Step 2: 补上组件用到的词条**

三个 `i18n/locales/*.ts` 各加一条（此时文件还是 `export default {}`，直接写成）：

`zh.ts`：
```ts
export default {
	lang: { unavailable: '该页暂无此语言版本' },
}
```

`en.ts`：
```ts
export default {
	lang: { unavailable: 'Not available in this language yet' },
}
```

`ja.ts`：
```ts
export default {
	lang: { unavailable: 'この言語版はまだありません' },
}
```

- [ ] **Step 3: 插进侧栏**

`app/components/blog/BlogSidebar.vue` 的 `<footer class="sidebar-footer">` 内，在 `<BlogThemeToggle />` **之前**插入一行：

```vue
	<footer class="sidebar-footer">
		<BlogLangToggle />
		<BlogThemeToggle />
		<ZIconNavList :list="appConfig.footer.iconNav" />
	</footer>
```

- [ ] **Step 4: 构建并验证**

Run: `pnpm build && pnpm preview`

Expected:
- 侧栏底部出现 `中 / En / あ` 胶囊，位于主题切换正上方，两者外观一致
- 当前语言按钮高亮
- 在只有中文的文章页上，`En` 与 `あ` 灰显且不可点，悬停提示「该页暂无此语言版本」
- 在 `/archive` 上三个都可点，点击后 URL 变为 `/en/archive`，cookie 更新

- [ ] **Step 5: 提交**

```bash
pnpm test && pnpm lint
git add -A
git commit -m "feat(i18n): 侧栏语言切换器"
```

---

## 阶段 D：界面文案

### Task 9: 词条基建与完整性闸门

Spec 原本打算用 `tsc` 当闸门，但仓库没装 vue-tsc、没有 typecheck script。改用 `node:test` 的键名对齐测试：既能抓漏翻也能抓多余键，贴合现有测试约定，不引入新依赖。TypeScript 类型继续提供编辑器内的即时反馈。

**Files:**
- Modify: `i18n/locales/zh.ts`
- Modify: `i18n/locales/en.ts`
- Modify: `i18n/locales/ja.ts`
- Create: `i18n/locales/parity.test.ts`
- Create: `i18n/i18n.d.ts`
- Create: `app/plugins/i18n-missing.client.ts`

**Interfaces:**
- Consumes: 无
- Produces: `MessageSchema` 类型（从 `zh.ts` 导出）；后续所有抽词条任务都往这三个文件里加键

- [ ] **Step 1: 写 parity 测试**

Create `i18n/locales/parity.test.ts`：

```ts
import assert from 'node:assert/strict'
// eslint-disable-next-line test/no-import-node-test -- Vitest is not a project dependency; use Node's built-in runner.
import test from 'node:test'

function flatten(obj: Record<string, unknown>, prefix = ''): string[] {
	return Object.entries(obj).flatMap(([k, v]) => {
		const key = prefix ? `${prefix}.${k}` : k
		return v && typeof v === 'object' && !Array.isArray(v)
			? flatten(v as Record<string, unknown>, key)
			: [key]
	})
}

test('en and ja carry exactly the keys zh defines', async () => {
	const zh = (await import('./zh.ts')).default
	const en = (await import('./en.ts')).default
	const ja = (await import('./ja.ts')).default

	const expected = flatten(zh).sort()
	assert.deepEqual(flatten(en).sort(), expected, 'en 与 zh 的键不一致')
	assert.deepEqual(flatten(ja).sort(), expected, 'ja 与 zh 的键不一致')
})

test('no message is left empty', async () => {
	for (const locale of ['zh', 'en', 'ja']) {
		const messages = (await import(`./${locale}.ts`)).default
		const walk = (obj: Record<string, unknown>, prefix = ''): void => {
			for (const [k, v] of Object.entries(obj)) {
				const key = prefix ? `${prefix}.${k}` : k
				if (v && typeof v === 'object')
					walk(v as Record<string, unknown>, key)
				else
					assert.ok(typeof v === 'string' && v.length > 0, `${locale}.${key} 是空词条`)
			}
		}
		walk(messages)
	}
})
```

- [ ] **Step 2: 跑测试确认通过**

Run: `node --test i18n/locales/parity.test.ts`
Expected: 2 tests pass（此时三个文件只有 `lang.unavailable` 一条，键是齐的）

- [ ] **Step 3: 给 zh.ts 导出类型**

`i18n/locales/zh.ts` 改为：

```ts
/**
 * 中文是词条的唯一真源。不写 as const —— 值需要推断成 string，
 * 否则 MessageSchema 会退化成字面量类型，另外两种语言无法满足。
 */
const zh = {
	lang: { unavailable: '该页暂无此语言版本' },
}

export default zh
export type MessageSchema = typeof zh
```

`i18n/locales/en.ts`：

```ts
import type { MessageSchema } from './zh'

const en: MessageSchema = {
	lang: { unavailable: 'Not available in this language yet' },
}

export default en
```

`i18n/locales/ja.ts`：

```ts
import type { MessageSchema } from './zh'

const ja: MessageSchema = {
	lang: { unavailable: 'この言語版はまだありません' },
}

export default ja
```

- [ ] **Step 4: 增广 vue-i18n 的键类型**

Create `i18n/i18n.d.ts`：

```ts
import type { MessageSchema } from './locales/zh'

declare module 'vue-i18n' {
	export interface DefineLocaleMessage extends MessageSchema {}
}

export {}
```

- [ ] **Step 5: 开发期漏键即报错**

没有 vue-tsc，模板里写错 key 只会在运行时渲染出原始 key。加一个开发期的 missing handler 把它变成显式错误。

Create `app/plugins/i18n-missing.client.ts`：

```ts
/**
 * 仓库没有 vue-tsc，模板里 $t('nav.archiv') 这类拼写错误无法在编译期抓到。
 * 开发环境下把 missing key 提升为抛错，让它在写的时候就暴露。
 * 生产环境保持 vue-i18n 默认行为（渲染原始 key），不因文案问题白屏。
 */
export default defineNuxtPlugin((nuxtApp) => {
	if (!import.meta.dev)
		return

	const i18n = nuxtApp.$i18n as { missing?: unknown }
	i18n.missing = (locale: string, key: string) => {
		throw new Error(`[i18n] 缺少词条：${locale}.${key}`)
	}
})
```

- [ ] **Step 6: 全量验证**

Run: `pnpm test && pnpm lint && pnpm build`
Expected: 全绿

- [ ] **Step 7: 提交**

```bash
git add -A
git commit -m "feat(i18n): 词条基建、键名对齐测试与开发期漏键告警"
```

---

### Task 10: 抽词条——侧栏、导航、页脚

从这里开始是机械劳动，按组件目录分批。每批的做法一致，先在本任务把流程走一遍。

**Files:**
- Modify: `app/components/blog/BlogSidebar.vue`
- Modify: `app/components/blog/*.vue`（含中文的）
- Modify: `app/app.config.ts`（nav、footer、iconNav 的中文文案）
- Modify: `i18n/locales/{zh,en,ja}.ts`

**Interfaces:**
- Consumes: Task 9 的 `MessageSchema`
- Produces: 词条命名空间 `nav.*`、`footer.*`、`sidebar.*`

- [ ] **Step 1: 列出本批文件**

Run:

```bash
grep -rlP '[\x{4e00}-\x{9fff}]' app/components/blog --include="*.vue"
```

把输出记下来，逐个处理。

- [ ] **Step 2: 逐个文件把界面文案换成 $t**

以 `BlogSidebar.vue:24` 为例。原文：

```vue
			<span class="nav-text">{{ debouncedSelection || searchStore.word || '搜索' }}</span>
```

改为：

```vue
			<span class="nav-text">{{ debouncedSelection || searchStore.word || $t('sidebar.search') }}</span>
```

同时在三个词条文件加：

```ts
// zh.ts
	sidebar: { search: '搜索' },
// en.ts
	sidebar: { search: 'Search' },
// ja.ts
	sidebar: { search: '検索' },
```

**只改界面文案，不动注释。** 注释保持中文。

- [ ] **Step 3: app.config.ts 的 nav 与 footer**

`app.config.ts` 里 `footer.nav` 的 `title`（`探索`、`社交`）与 `items[].text`（`Atom订阅`、`开往` 等）是中文字面量。把它们换成词条 key，在消费处 `$t()`：

```ts
	footer: {
		nav: [
			{
				title: 'footer.explore',
				items: [
					{ icon: 'tabler:rss', text: 'footer.atom', url: '/atom.xml' },
					{ icon: 'ri:subway-line', text: 'footer.travellings', url: 'https://www.travellings.cn/go.html' },
				],
			},
		],
	},
```

`BlogSidebar.vue` 与页脚组件里渲染这些字段的地方套 `$t()`：

```vue
			<h3 v-if="group.title">
				{{ $t(group.title) }}
			</h3>
```

人名与站点名（`GitHub: senshinya`、邮箱等）保持原样，不进词条表。

- [ ] **Step 4: 验证本批无残留**

Run:

```bash
grep -rnP '[\x{4e00}-\x{9fff}]' app/components/blog --include="*.vue" \
  | grep -vP ':\s*(//|\*|/\*|<!--)' \
  | grep -v 'i18n/locales'
```

Expected: 输出只剩注释行。逐行确认没有漏掉的界面文案。

- [ ] **Step 5: 三语言测试与构建**

Run: `pnpm test && pnpm lint && pnpm build`
Expected: parity 测试通过（说明 en/ja 都补齐了）；构建成功。

- [ ] **Step 6: 提交**

```bash
git add -A
git commit -m "feat(i18n): 抽取侧栏与页脚文案"
```

---

### Task 11: 抽词条——文章相关组件

**Files:**
- Modify: `app/components/post/*.vue`
- Modify: `app/components/content/*.vue`
- Modify: `i18n/locales/{zh,en,ja}.ts`

**Interfaces:**
- Produces: 词条命名空间 `post.*`、`content.*`

- [ ] **Step 1: 列出本批文件**

Run: `grep -rlP '[\x{4e00}-\x{9fff}]' app/components/post app/components/content --include="*.vue"`

- [ ] **Step 2: 逐个替换**

已知的几处，照 Task 10 的方式处理：

| 文件:行 | 原文 | 建议 key |
|---|---|---|
| `PostFooter.vue:28` | `参考链接` | `post.references` |
| `PostFooter.vue:38` | `许可协议` | `post.license` |
| `PostFooter.vue:41-44` | `本文采用 … 许可协议，转载请注明出处。` | `post.licenseNotice`（带 `{link}` 插值） |
| `PostHeader.vue:26` | `文字分享` | `post.share` |
| `PostHeader.vue:35` | `创建于${d}` | `post.createdAt`（带 `{date}` 插值） |
| `PostHeader.vue:47` | `字` | `post.words`（写成 `{n} 字`） |
| `Article.vue:33` | `字` | 同上，复用 `post.words` |
| `PostSurround.vue:40` | `新故事即将发生` | `post.surroundNext` |
| `PostSurround.vue:44` | `已抵达博客尽头` | `post.surroundEnd` |
| `Filter.vue:21,27` | `全部分类` | `post.allCategories` |

带插值的写法，以 `PostHeader.vue:35` 为例。原文：

```vue
				:tip-transform="d => `创建于${d}`"
```

改为：

```vue
				:tip-transform="d => $t('post.createdAt', { date: d })"
```

词条：

```ts
// zh.ts
	post: { createdAt: '创建于{date}' },
// en.ts
	post: { createdAt: 'Created on {date}' },
// ja.ts
	post: { createdAt: '{date}に作成' },
```

- [ ] **Step 3: 验证无残留**

Run:

```bash
grep -rnP '[\x{4e00}-\x{9fff}]' app/components/post app/components/content --include="*.vue" \
  | grep -vP ':\s*(//|\*|/\*|<!--)'
```

Expected: 只剩注释行。

- [ ] **Step 4: 测试构建提交**

```bash
pnpm test && pnpm lint && pnpm build
git add -A
git commit -m "feat(i18n): 抽取文章组件文案"
```

---

### Task 12: 抽词条——页面、错误页与剩余组件

**Files:**
- Modify: `app/pages/**/*.vue`
- Modify: `app/error.vue`
- Modify: `app/components/{comment,media,memo,travel,util,widget,popover,partial}/*.vue`
- Modify: `app/app.config.ts`
- Modify: `i18n/locales/{zh,en,ja}.ts`

**Interfaces:**
- Produces: 词条命名空间 `page.*`、`error.*`、`comment.*`、`media.*`、`memo.*`、`travel.*`、`search.*`

- [ ] **Step 1: 列出剩余文件**

Run:

```bash
grep -rlP '[\x{4e00}-\x{9fff}]' app --include="*.vue" --include="*.ts" \
  | grep -v 'i18n/locales' | sort
```

- [ ] **Step 2: 逐个替换**

已知的几处：

| 文件:行 | 原文 | 建议 key |
|---|---|---|
| `error.vue:22` | `返回主页` | `error.backHome` |
| `error.vue:23` | `尝试忽略` | `error.ignore` |
| `pages/memos/index.vue:14` | `碎语` | `page.memos.title` |
| `pages/memos/index.vue:15` | `…的碎碎念，一些来不及写成文章的短想法。` | `page.memos.description`（带 `{site}` 插值） |
| `app.config.ts` | `stats.wordCount: '约7万'` | `stats.wordCount` |

页面的 `useSeoMeta` 需要在 setup 里取 `t`：

```ts
const { t } = useI18n()
useSeoMeta({
	title: () => t('page.memos.title'),
	description: () => t('page.memos.description', { site: appConfig.title }),
})
```

写成 getter，语言切换时才会跟着更新。

- [ ] **Step 3: 全站残留检查**

Run:

```bash
grep -rnP '[\x{4e00}-\x{9fff}]' app --include="*.vue" --include="*.ts" \
  | grep -v 'i18n/locales' \
  | grep -vP ':\s*(//|\*|/\*|<!--)' \
  | grep -vP "\\$t\\("
```

Expected: 输出为空，或只剩确认过不需要翻译的项（人名、站点名、品牌名）。把这些例外记在提交信息里。

本检查只覆盖 `app/`。`server/routes/atom.xml.get.ts` 里还有一句硬编码的「点击查看全文」，以及取自中文 `blogConfig` 的 title／subtitle／description —— 那些由 Task 17 处理，此处不必动。

- [ ] **Step 4: 测试构建提交**

```bash
pnpm test && pnpm lint && pnpm build
git add -A
git commit -m "feat(i18n): 抽取页面与剩余组件文案"
```

---

## 阶段 E：内容与数据

### Task 13: 分类改成语言无关 id

**Files:**
- Modify: `blog.config.ts`（article.categories、defaultCategory）
- Modify: `content/zh/posts/**/*.md`（44 个文件的 frontmatter）
- Modify: `app/components/post/Filter.vue`
- Modify: `i18n/locales/{zh,en,ja}.ts`

**Interfaces:**
- Consumes: 无
- Produces: 分类 id `fiddling` / `projects` / `notes` / `daily`；词条命名空间 `category.*`

- [ ] **Step 1: 改配置**

`blog.config.ts` 的 `article.categories` 替换为：

```ts
		/** 分类 id 与展示名解耦：id 语言无关，展示名在 i18n/locales/*.ts 的 category.* */
		categories: {
			/** 折腾：网络/代理/自建服务/系统与硬件 */
			fiddling: { icon: 'tabler:tool', color: '#33aaff' },
			/** 项目：自己写的东西、造的轮子 */
			projects: { icon: 'tabler:code', color: '#7777ff' },
			/** 笔记：学习记录、原理梳理 */
			notes: { icon: 'tabler:notebook', color: '#33bbaa' },
			/** 日常：生活片段与杂感 */
			daily: { icon: 'tabler:leaf', color: '#ff7777' },
		},
```

`basicConfig` 里 `defaultCategory: '笔记'` 改为 `defaultCategory: 'notes'`。

- [ ] **Step 2: 加分类词条**

```ts
// zh.ts
	category: { fiddling: '折腾', projects: '项目', notes: '笔记', daily: '日常' },
// en.ts
	category: { fiddling: 'Fiddling', projects: 'Projects', notes: 'Notes', daily: 'Daily' },
// ja.ts
	category: { fiddling: '弄り', projects: 'プロジェクト', notes: 'ノート', daily: '日常' },
```

- [ ] **Step 3: 批量改 frontmatter**

```bash
cd content/zh/posts
for f in $(grep -rl '^categories:' . --include="*.md"); do
	sed -i '' \
		-e 's/^\(categories:.*\)折腾/\1fiddling/' \
		-e 's/^\(categories:.*\)项目/\1projects/' \
		-e 's/^\(categories:.*\)笔记/\1notes/' \
		-e 's/^\(categories:.*\)日常/\1daily/' "$f"
done
cd -
```

- [ ] **Step 4: 确认改干净了**

Run:

```bash
grep -rn '^categories:' content/zh/posts --include="*.md" | grep -P '[\x{4e00}-\x{9fff}]'
```

Expected: 输出为空。

再确认四个 id 都出现过：

```bash
grep -rhn '^categories:' content/zh/posts --include="*.md" | sort -u
```

- [ ] **Step 5: 展示处套 t()**

`app/components/post/Filter.vue:21` 与 `:27` 的 `{{ category ?? '全部分类' }}` 与 `<span>全部分类</span>`：

```vue
			<span class="filter-text">{{ category ? $t(`category.${category}`) : $t('post.allCategories') }}</span>
```

```vue
				<span>{{ $t('post.allCategories') }}</span>
```

下拉列表里每个分类项同样套 `$t(\`category.${item}\`)`。

**注意**：`tags` 字段不改。tags 是自由文本，不是受控枚举。

- [ ] **Step 6: 验证**

Run: `pnpm test && pnpm lint && pnpm build`

再手动确认分类筛选仍能工作、图标与颜色仍正确：

```bash
pnpm preview   # 打开首页，逐个点分类筛选
```

- [ ] **Step 7: 提交**

```bash
git add -A
git commit -m "feat(i18n): 分类改用语言无关 id，展示名进词条表"
```

---

### Task 14: 游记按语言拆目录

**Files:**
- Modify: `app/travels/index.ts`
- Move: `app/travels/*.yaml` → `app/travels/zh/*.yaml`
- Modify: `nuxt.config.ts:18-26`（删掉自己推导游记路由的那段）
- Modify: `modules/i18n-manifest/index.ts`（游记扫描已在 Task 4 实现，此处确认路径）
- Modify: `app/travels/draft.test.ts`（若受影响）

**Interfaces:**
- Consumes: Task 4 的清单模块
- Produces: `getTravels(locale: string): Travel[]`、`getTravelBySlug(locale: string, slug: string): Travel | undefined`

- [ ] **Step 1: 挪文件**

```bash
mkdir -p app/travels/zh app/travels/en app/travels/ja
git mv app/travels/kansai-202504.yaml app/travels/zh/
git mv app/travels/korea-202510.yaml app/travels/zh/
touch app/travels/en/.gitkeep app/travels/ja/.gitkeep
```

- [ ] **Step 2: 改注册表用 glob 自动发现**

`app/travels/index.ts` 整体替换：

```ts
import type { Travel } from '~/types/travel'
import { getVisibleTravels } from './draft'

/**
 * 游记注册表。放一个 <slug>.yaml 进 app/travels/<locale>/ 即生效，
 * 不需要手工登记 —— 预渲染路由由 modules/i18n-manifest 扫描同一批文件产出，
 * 两处不会再各自维护一份事实。
 *
 * unplugin-yaml 把 *.yaml 声明为 Record<string, unknown>，故需断言。
 */
const modules = import.meta.glob<Record<string, unknown>>('./*/*.yaml', { eager: true, import: 'default' })

const byLocale = new Map<string, Travel[]>()

for (const [path, data] of Object.entries(modules)) {
	const locale = path.split('/')[1]!
	const list = byLocale.get(locale) ?? []
	list.push(data as unknown as Travel)
	byLocale.set(locale, list)
}

export function getTravels(locale: string) {
	const visible = getVisibleTravels(byLocale.get(locale) ?? [], import.meta.dev)
	return visible.toSorted((a, b) => b.published.localeCompare(a.published))
}

export function getTravelBySlug(locale: string, slug: string) {
	return getTravels(locale).find(travel => travel.slug === slug)
}
```

- [ ] **Step 3: 更新调用处**

Run: `grep -rn "getTravelBySlug\|from '~/travels'\|from '../travels'" app --include="*.vue" --include="*.ts"`

`app/pages/travels/index.ts` 与 `app/pages/travels/[slug].vue` 里改成传当前 locale：

```ts
const { locale } = useI18n()
const travels = computed(() => getTravels(locale.value))
```

```ts
const travel = computed(() => getTravelBySlug(locale.value, route.params.slug as string))
```

`[slug].vue` 里取不到时抛 404：

```ts
if (!travel.value)
	throw createError({ statusCode: 404 })
```

- [ ] **Step 4: 删掉 nuxt.config 里的游记路由推导**

`nuxt.config.ts` 顶部删除这一段（清单模块已接管）：

```ts
const travelDirectory = resolve('./app/travels')
const travelRoutes = readdirSync(travelDirectory)
	.filter(file => file.endsWith('.yaml'))
	.filter(file => !isTravelDraftSource(readFileSync(resolve(travelDirectory, file), 'utf8')))
	.map(file => `/travels/${basename(file, '.yaml')}`)
```

连同 `nitro.prerender.routes` 里引用 `travelRoutes` 的地方，以及不再使用的 `readdirSync` / `readFileSync` / `basename` / `isTravelDraftSource` import。

- [ ] **Step 5: 验证**

Run:

```bash
pnpm test && pnpm lint && pnpm build
ls .output/public/travels/kansai-202504/index.html
ls .output/public/travels/korea-202510/index.html
```

Expected: 两篇游记的中文产物路径不变；测试全绿。

- [ ] **Step 6: 提交**

```bash
git add -A
git commit -m "feat(i18n): 游记按语言分目录，注册表改用 glob 自动发现"
```

---

### Task 15: 按 locale 取 collection

Task 2 里把 8 处 `queryCollection` 硬编码成了 `content_zh`，现在改成跟随当前语言。

**Files:**
- Modify: `app/composables/useArticle.ts`
- Modify: `app/pages/[...slug].vue`
- Modify: `app/pages/index.vue`
- Modify: `app/pages/friends.vue`
- Modify: `app/components/post/PostSurround.vue`
- Modify: `app/components/popover/Search.vue`
- Modify: `server/api/stats.get.ts`
- Modify: `server/routes/atom.xml.get.ts`

**Interfaces:**
- Produces: `useContentCollection(): ComputedRef<keyof Collections>`

- [ ] **Step 1: 加一个取 collection 名的 composable**

在 `app/composables/useArticle.ts` 顶部加：

```ts
import type { Collections } from '@nuxt/content'

/** 当前语言对应的 content collection 名。 */
export function useContentCollection() {
	const { locale } = useI18n()
	return computed(() => `content_${locale.value}` as keyof Collections)
}
```

- [ ] **Step 2: 改客户端调用处**

`getArticleIndexOptions` 需要拿到 collection 名，改签名：

```ts
export function getArticleIndexOptions(collection: keyof Collections, path = 'posts/%') {
	return queryCollection(collection)
		.where('stem', 'LIKE', path)
		.select('categories', 'date', 'description', 'image', 'path', 'readingTime', 'recommend', 'tags', 'title', 'type')
		.all()
}
```

调用处传入 `useContentCollection().value`。

`app/pages/[...slug].vue`：

```ts
const collection = useContentCollection()
const { data: page } = await useAsyncData(
	() => `content:${route.path}`,
	() => queryCollection(collection.value).path(stripLocale(route.path, LOCALES, 'zh').basePath).first(),
	{ watch: [collection] },
)
if (!page.value)
	throw createError({ statusCode: 404 })
```

`app/components/post/PostSurround.vue`：

```ts
const collection = useContentCollection()
const { data: surroundings } = await useAsyncData(
	() => `surround:${route.path}`,
	() => queryCollectionItemSurroundings(
		collection.value,
		stripLocale(route.path, LOCALES, 'zh').basePath,
		{ fields: ['date', 'title', 'type'] },
	),
	{ watch: [collection] },
)
```

`app/components/popover/Search.vue`：

```ts
const collection = useContentCollection()
const { data: sections } = await useAsyncData(
	() => `search:${collection.value}`,
	() => queryCollectionSearchSections(collection.value),
	{ watch: [collection] },
)
```

两处都需要在文件顶部加：

```ts
import blogConfig from '~~/blog.config'
import { stripLocale } from '~/utils/locale'

const LOCALES = blogConfig.locales.map(l => l.code)
```

- [ ] **Step 3: 改服务端调用处**

`server/api/stats.get.ts` 与 `server/routes/atom.xml.get.ts` 拿不到 `useI18n`。stats 先固定统计中文（`content_zh`），atom 在 Task 17 拆成三份时再处理。此步只需确认它们仍指向 `content_zh` 且能跑通。

- [ ] **Step 4: 验证**

Run: `pnpm test && pnpm lint && pnpm build`

```bash
pnpm preview
```

中文站行为应与改动前完全一致。`/en/` 与 `/ja/` 下的列表页此时为空（还没有译文），属正常。

- [ ] **Step 5: 提交**

```bash
git add -A
git commit -m "feat(i18n): content collection 跟随当前语言"
```

---

## 阶段 F：周边适配

### Task 16: routeRules 与预渲染按语言展开

**Files:**
- Modify: `nuxt.config.ts`（routeRules）

**Interfaces:**
- Consumes: `blogConfig.locales`

- [ ] **Step 1: 改成程序化展开**

`nuxt.config.ts` 顶部加一个辅助函数：

```ts
/**
 * 把按路径写死的 routeRules 按语言展开。加了语言前缀之后，
 * 只写 '/media' 会漏掉 /en/media 与 /ja/media —— 那两个会退回默认行为，
 * 失败得很安静（/media 依赖 ssr:false 才能让首帧读到 route.query）。
 */
function localizeRules(rules: Record<string, unknown>) {
	const prefixes = ['', ...blogConfig.locales.filter(l => l.code !== 'zh').map(l => `/${l.code}`)]
	return Object.fromEntries(
		Object.entries(rules).flatMap(([path, rule]) =>
			prefixes.map(prefix => [`${prefix}${path}`, rule] as const),
		),
	)
}
```

`routeRules` 里，把与语言相关的那几条包进去：

```ts
	routeRules: {
		'/api/og': { prerender: false },
		'/api/stats': { prerender: true, headers: { 'Content-Type': 'application/json' } },
		'/subscriptions.opml': { prerender: true, headers: { 'Content-Type': 'application/xml' } },
		...localizeRules({
			'/media': { ssr: false },
			'/memos/**': { isr: 600 },
			'/atom.xml': { prerender: true, headers: { 'Content-Type': 'application/xml' } },
		}),
	},
```

`/api/*` 与 `/subscriptions.opml` 不展开——它们没有语言前缀版本。

- [ ] **Step 2: 预渲染路由里补上 /media 的语言版本**

`nitro.prerender.routes` 里原本显式登记了 `/media`，改为：

```ts
			...['', '/en', '/ja'].map(prefix => `${prefix}/media`),
```

- [ ] **Step 3: 验证展开结果**

Run: `pnpm build`

```bash
ls .output/public/media/index.html .output/public/en/media/index.html .output/public/ja/media/index.html
```

Expected: 三份都在。

- [ ] **Step 4: 提交**

```bash
pnpm test && pnpm lint
git add -A
git commit -m "fix(i18n): routeRules 与预渲染路由按语言展开"
```

---

### Task 17: 三份订阅源

**Files:**
- Create: `server/utils/feed.ts`
- Modify: `server/routes/atom.xml.get.ts`
- Create: `server/routes/en/atom.xml.get.ts`
- Create: `server/routes/ja/atom.xml.get.ts`
- Modify: `nuxt.config.ts`（head 的 alternate link）

**Interfaces:**
- Consumes: Task 9 的词条文件
- Produces: `buildAtomFeed(event: H3Event, locale: 'zh' | 'en' | 'ja'): Promise<string>`；词条命名空间 `site.*`

- [ ] **Step 0: 先给站点元信息加词条**

feed 的 title／subtitle／description 目前全部取自中文的 `blogConfig`，`renderContent` 里还有一句硬编码的「点击查看全文」。英日订阅源不能照搬，先把这四条进词条表。

```ts
// zh.ts
	site: {
		title: '信也のブログ',
		subtitle: '一写代码的',
		description: '互联网自留地。shinya 的个人博客，记录折腾、项目、笔记与日常 —— 网络与代理、自建服务、Go 与系统底层、以及那些不知道为什么就搞了一整个周末的东西。',
		readFull: '点击查看全文',
	},
// en.ts
	site: {
		title: "Shinya's Blog",
		subtitle: 'Someone who writes code',
		description: "A corner of the internet. Shinya's personal blog on tinkering, projects, notes and daily life — networking and proxies, self-hosted services, Go and systems internals, and things that somehow ate an entire weekend.",
		readFull: 'Read the full post',
	},
// ja.ts
	site: {
		title: '信也のブログ',
		subtitle: 'コードを書く人',
		description: 'インターネットの片隅。shinya の個人ブログ。いじり、プロジェクト、ノート、日常 —— ネットワークとプロキシ、セルフホスト、Go とシステム内部、そして気づけば週末を丸ごと溶かしていたあれこれ。',
		readFull: '全文を読む',
	},
```

`blog.config.ts` 里的 `title` / `subtitle` / `description` 保留不动——`@nuxtjs/seo` 的 `site.name` 与构建期配置仍要用它们，它们代表默认语言。

- [ ] **Step 1: 把现有 atom 逻辑抽成共用 builder**

Create `server/utils/feed.ts`，内容是把 `server/routes/atom.xml.get.ts` 整体挪过来并参数化。`server/utils/` 下的导出会被 Nitro 自动导入，但这里保留显式 import 以便阅读。

```ts
import type { ContentCollectionItem, Collections } from '@nuxt/content'
import type { H3Event } from 'h3'
import { pascalCase } from 'es-toolkit/string'
import XmlBuilder from 'fast-xml-builder'
import { Temporal } from 'temporal-polyfill'
import blogConfig from '~~/blog.config'
import en from '~~/i18n/locales/en'
import ja from '~~/i18n/locales/ja'
import zh from '~~/i18n/locales/zh'
import packageJson from '~~/package.json'
import { toZonedTemporal } from '~~/shared/utils/time'

const MESSAGES = { zh, en, ja } as const

const builder = new XmlBuilder({
	attributeNamePrefix: '$',
	cdataPropName: '$',
	format: true,
	ignoreAttributes: false,
	textNodeName: '_',
})

function formatIsoDate(date?: string) {
	if (!date)
		return
	try {
		return toZonedTemporal(date).toInstant().toString()
	}
	catch {
		console.error('Invalid date format', date)
		return date
	}
}

export async function buildAtomFeed(event: H3Event, locale: keyof typeof MESSAGES) {
	const collection = `content_${locale}` as keyof Collections
	const site = MESSAGES[locale].site
	const language = blogConfig.locales.find(l => l.code === locale)!.language
	// 默认语言不带前缀，其余语言的页面与 feed 自身都在 /<locale> 下
	const prefix = locale === 'zh' ? '' : `/${locale}`

	const getUrl = (path: string | undefined) =>
		new URL(`${prefix}${path ?? ''}`, blogConfig.url).toString()

	const renderContent = (post: ContentCollectionItem) => [
		post.image && `<img src="${post.image}" alt="${post.title}" />`,
		post.description && `<p>${post.description}</p>`,
		`<a class="view-full" href="${getUrl(post.path)}" target="_blank">${site.readFull}</a>`,
	].join(' ')

	const posts = await queryCollection(event, collection)
		.where('stem', 'LIKE', 'posts/%')
		.order('date', 'DESC')
		.limit(blogConfig.feed.limit)
		.all()

	const entries = posts.map(post => ({
		id: getUrl(post.path),
		title: post.title ?? '',
		// Atom 的 <updated> 是必填元素（RFC 4287 §4.2.15），不能因为字段删了就不发。
		// 改用创建日期 —— 原先这里取的 post.updated 其实一篇文章都没写过。
		updated: formatIsoDate(post.date),
		author: { name: post.author || blogConfig.author.name },
		content: {
			$type: 'html',
			$: renderContent(post),
		},
		link: { $href: getUrl(post.path) },
		summary: post.description,
		category: { $term: post.categories?.[0] },
		published: formatIsoDate(post.published ?? post.date),
	}))

	const runtimeConfig = useRuntimeConfig()

	const feed = {
		$xmlns: 'http://www.w3.org/2005/Atom',
		id: new URL(`${prefix}/`, blogConfig.url).toString(),
		title: site.title,
		updated: runtimeConfig.public.buildTime,
		description: site.description, // RSS 2.0
		author: {
			name: blogConfig.author.name,
			email: blogConfig.author.email,
			uri: blogConfig.author.homepage,
		},
		link: [
			{ $href: new URL(`${prefix}/atom.xml`, blogConfig.url).toString(), $rel: 'self' },
			{ $href: new URL(`${prefix}/`, blogConfig.url).toString(), $rel: 'alternate' },
		],
		language, // RSS 2.0
		generator: {
			$uri: 'https://github.com/L33Z22L11/blog-v3',
			$version: packageJson.version,
			_: pascalCase(packageJson.name),
		},
		icon: blogConfig.favicon,
		logo: blogConfig.author.avatar, // Ratio should be 2:1
		rights: `© ${Temporal.Now.plainDateISO().year.toString()} ${blogConfig.author.name}`,
		subtitle: site.subtitle,
		entry: entries,
	}

	return builder.build({
		'?xml': { $version: '1.0', $encoding: 'UTF-8' },
		'?xml-stylesheet': blogConfig.feed.enableStyle ? { $type: 'text/xsl', $href: '/assets/atom.xsl' } : undefined,
		feed,
	})
}
```

**注意**：原文件里 `const runtimeConfig = useRuntimeConfig()` 在模块顶层，抽成函数后必须挪进函数体内，否则在 Nitro 的模块求值阶段会拿不到上下文。

- [ ] **Step 2: 三个薄路由**

`server/routes/atom.xml.get.ts`：

```ts
import { buildAtomFeed } from '../utils/feed'

export default defineEventHandler(event => buildAtomFeed(event, 'zh'))
```

`server/routes/en/atom.xml.get.ts`：

```ts
import { buildAtomFeed } from '../../utils/feed'

export default defineEventHandler(event => buildAtomFeed(event, 'en'))
```

`server/routes/ja/atom.xml.get.ts`：

```ts
import { buildAtomFeed } from '../../utils/feed'

export default defineEventHandler(event => buildAtomFeed(event, 'ja'))
```

- [ ] **Step 3: head 里的 alternate 指向当前语言**

`nuxt.config.ts` 的 `app.head.link` 里那条写死的：

```ts
				{ rel: 'alternate', type: 'application/atom+xml', href: '/atom.xml' },
```

删掉，改到 `app.vue` 或默认布局里用 `useHead` 按 locale 生成：

```ts
const { locale } = useI18n()
useHead({
	link: [{
		rel: 'alternate',
		type: 'application/atom+xml',
		href: () => locale.value === 'zh' ? '/atom.xml' : `/${locale.value}/atom.xml`,
	}],
})
```

- [ ] **Step 4: 验证**

Run: `pnpm build`

```bash
ls .output/public/atom.xml .output/public/en/atom.xml .output/public/ja/atom.xml
grep -o '<language>[^<]*' .output/public/atom.xml       # zh-CN
grep -o '<language>[^<]*' .output/public/ja/atom.xml    # ja-JP
grep -o '<subtitle>[^<]*' .output/public/en/atom.xml    # 应为英文，不是「一写代码的」
grep -o 'view-full[^<]*' .output/public/en/atom.xml     # 应为 Read the full post
```

Expected: 三份都在；language 分别是 `zh-CN` 与 `ja-JP`；英文那份的 subtitle 与「查看全文」链接文案都是英文。

- [ ] **Step 5: 提交**

```bash
pnpm test && pnpm lint
git add -A
git commit -m "feat(i18n): 三语言各一份 Atom 订阅源"
```

---

### Task 18: hreflang 由清单驱动

@nuxtjs/i18n 会为所有配置的 locale 自动发 hreflang，包括那些页面根本没有译文的语言。必须接管。

**Files:**
- Modify: `nuxt.config.ts`（i18n.baseUrl、关闭自动 SEO head）
- Create: `app/composables/useLocaleAlternates.ts`
- Modify: `app/layouts/default.vue`

**Interfaces:**
- Consumes: `#build/i18n-manifest`、`stripLocale`
- Produces: `useLocaleAlternates(): void`（副作用式，内部调 useHead）

- [ ] **Step 1: 关掉 i18n 的自动 SEO head**

`nuxt.config.ts` 的 `i18n` 段里加：

```ts
		baseUrl: blogConfig.url,
		seo: false,
```

- [ ] **Step 2: 自己发 alternate**

Create `app/composables/useLocaleAlternates.ts`：

```ts
import { manifest } from '#build/i18n-manifest'
import blogConfig from '~~/blog.config'
import { stripLocale } from '~/utils/locale'

const LOCALES = blogConfig.locales.map(l => l.code)

/**
 * 只为真实存在译文的语言发 hreflang。
 * i18n 自带的 seo 会为所有配置的 locale 都发一条，包括不存在的页面，
 * 那会让 hreflang 说谎，故关掉它由这里接管。
 */
export function useLocaleAlternates() {
	const route = useRoute()

	useHead(() => {
		const { basePath } = stripLocale(route.path, LOCALES, 'zh')
		const available = manifest[basePath] ?? LOCALES
		const href = (code: string) => new URL(
			code === 'zh' ? basePath : `/${code}${basePath}`,
			blogConfig.url,
		).toString()

		return {
			link: [
				...available.map(code => ({
					rel: 'alternate',
					hreflang: blogConfig.locales.find(l => l.code === code)!.language,
					href: href(code),
				})),
				{ rel: 'alternate', hreflang: 'x-default', href: href('zh') },
			],
		}
	})
}
```

- [ ] **Step 3: 在布局里调用**

`app/layouts/default.vue` 的 `<script setup>` 里加：

```ts
useLocaleAlternates()
```

- [ ] **Step 4: 验证**

Run: `pnpm build`

```bash
grep -o 'hreflang="[^"]*"' .output/public/daily/anti-chronic-gastritis/index.html | sort -u
```

Expected: 只有 `zh-CN` 与 `x-default`（这篇尚无译文）。若某篇有英文版，则应额外出现 `en-US`。

- [ ] **Step 5: 提交**

```bash
pnpm test && pnpm lint
git add -A
git commit -m "feat(i18n): hreflang 由清单驱动，只列真实存在的语言"
```

---

### Task 19: 日文字体

**Files:**
- Modify: `nuxt.config.ts`（移除写死的字体 link）
- Modify: `app/layouts/default.vue` 或新建 `app/composables/useLocaleFonts.ts`
- Modify: `app/assets/css/font.scss`

- [ ] **Step 1: 按 locale 注入 JP 字体**

Create `app/composables/useLocaleFonts.ts`：

```ts
/**
 * JP 字体只在日语页面注入。站点按语言分别预渲染，
 * 因此中英文页面的 head 里不会出现这条 link，读者不承担这份流量。
 */
export function useLocaleFonts() {
	const { locale } = useI18n()

	useHead(() => locale.value !== 'ja'
		? {}
		: {
			link: [{
				rel: 'stylesheet',
				href: 'https://fonts.googleapis.cn/css2?family=Noto+Sans+JP:wght@100..900&family=Noto+Serif+JP:wght@200..900&display=swap',
			}],
		})
}
```

在 `app/layouts/default.vue` 调用 `useLocaleFonts()`。

- [ ] **Step 2: 加 :lang(ja) 的字体覆盖**

`app/assets/css/font.scss` 末尾追加：

```scss
/* 中日共用汉字字形不同（直、骨、今、令 等），日语页面须落到 JP 字库。
   --font-creative 是抖音美好体（GB 字库，无假名覆盖），给它配 JP 兜底。 */
:lang(ja) {
	--font-basic: "Noto Sans JP", "InterVariable", sans-serif;
	--font-serif: "Noto Serif JP", serif;
	--font-creative: "DOUYINSANSBOLD-GB", "Noto Sans JP", "InterVariable", sans-serif;
	--font-stroke-free: "DOUYINSANSBOLD-GB", "Noto Sans JP", "Inter", sans-serif;
}
```

- [ ] **Step 3: 验证**

Run: `pnpm build`

```bash
grep -c 'Noto+Serif+JP' .output/public/ja/index.html      # 应为 1
grep -c 'Noto+Serif+JP' .output/public/index.html         # 应为 0
```

再肉眼确认切换器上的 `あ` 用的是日文字形（`LangToggle` 已给按钮挂 `:lang`）。

- [ ] **Step 4: 提交**

```bash
pnpm test && pnpm lint
git add -A
git commit -m "feat(i18n): 日语页面加载 JP 字体"
```

---

### Task 20: 搜索与日期的 locale 化

**Files:**
- Modify: `app/components/popover/Search.vue:10`
- Modify: `app/components/util/Date.vue:58`
- Modify: `app/components/content/FeedCard.vue:79`
- Modify: `app/pages/memos/[id].vue`

- [ ] **Step 1: 搜索分词跟随 locale**

`app/components/popover/Search.vue:10` 原文：

```ts
const segmenter = Intl.Segmenter && new Intl.Segmenter(appConfig.language, { granularity: 'word' })
```

改为：

```ts
const { locale } = useI18n()
const segmenter = computed(() => Intl.Segmenter
	&& new Intl.Segmenter(blogConfig.locales.find(l => l.code === locale.value)?.language, { granularity: 'word' }))
```

消费处相应改成 `segmenter.value`。

- [ ] **Step 2: 日期跟随 locale**

`app/components/util/Date.vue:58` 的 `:locale="blogConfig.language"` 改为：

```vue
		:locale="currentLanguage"
```

script 段加：

```ts
const { locale } = useI18n()
const currentLanguage = computed(() =>
	blogConfig.locales.find(l => l.code === locale.value)?.language ?? blogConfig.language)
```

`FeedCard.vue:79` 与 `memos/[id].vue` 同样处理。注释里说明 locale 必须显式给的那几段保留，把「取 blogConfig.language」改成「取当前语言」。

**`blogConfig.timeZone` 不动** —— 它描述作者发文时区，与读者语言无关。

- [ ] **Step 3: 验证**

Run: `pnpm build && pnpm preview`

Expected: 中文页日期格式与改动前一致；`/en/archive` 上日期显示为英文格式；`/ja/archive` 为日文格式。

- [ ] **Step 4: 提交**

```bash
pnpm test && pnpm lint
git add -A
git commit -m "feat(i18n): 搜索分词与日期格式跟随当前语言"
```

---

## 阶段 G：收尾验证

### Task 21: 端到端验证

**Files:**
- Create: `scripts/verify-i18n.ts`（一次性验证脚本，可保留）

- [ ] **Step 1: 写验证脚本**

Create `scripts/verify-i18n.ts`：

```ts
/** 构建产物的 i18n 断言。跑法：pnpm build && unrun scripts/verify-i18n.ts */
import assert from 'node:assert/strict'
import { existsSync, readFileSync } from 'node:fs'
import { resolve } from 'node:path'

const out = resolve('.output/public')
const at = (p: string) => resolve(out, p.replace(/^\//, ''))

// 中文 URL 逐字节不变
for (const p of ['/daily/anti-chronic-gastritis', '/travels/kansai-202504', '/archive', '/friends']) {
	assert.ok(existsSync(at(`${p}/index.html`)), `中文路径丢失：${p}`)
}

// 语言前缀页面存在
for (const p of ['/en/archive', '/ja/archive', '/en/media', '/ja/media']) {
	assert.ok(existsSync(at(`${p}/index.html`)), `前缀路径缺失：${p}`)
}

// 未翻译的文章不应生成前缀版本
assert.ok(!existsSync(at('/en/daily/anti-chronic-gastritis/index.html')), '未翻译文章不应有 /en 产物')

// 三份订阅源
for (const p of ['/atom.xml', '/en/atom.xml', '/ja/atom.xml'])
	assert.ok(existsSync(at(p)), `订阅源缺失：${p}`)

// hreflang 只列真实存在的语言
const html = readFileSync(at('/daily/anti-chronic-gastritis/index.html'), 'utf8')
const tags = [...html.matchAll(/hreflang="([^"]+)"/g)].map(m => m[1]).sort()
assert.deepEqual(tags, ['x-default', 'zh-CN'], `hreflang 不应包含无译文的语言，实际：${tags.join(',')}`)

// JP 字体只在日语页
assert.ok(readFileSync(at('/ja/index.html'), 'utf8').includes('Noto+Serif+JP'))
assert.ok(!readFileSync(at('/index.html'), 'utf8').includes('Noto+Serif+JP'))

console.info('✓ i18n 构建产物验证通过')
```

- [ ] **Step 2: 跑全套**

Run:

```bash
pnpm test && pnpm lint && pnpm build && unrun scripts/verify-i18n.ts
```

Expected: 全绿。

- [ ] **Step 3: 视觉验证**

按仓库既有做法（生产构建 + puppeteer-core 驱动系统 Chrome，dev 下 headless 会卡在 Vite HMR）：

```bash
pnpm preview
```

逐项确认：
- 语言切换器位于主题切换正上方，两者外观一致，浅色深色都对
- 只有中文的文章页上 `En` `あ` 灰显不可点
- `/archive` 上三个都可点，切换后 URL 与界面语言同步
- 日语页面正文汉字为 JP 字形
- 无痕窗口设浏览器语言为日语访问 `/`，跳到 `/ja` 且 cookie 写入；刷新不再跳

- [ ] **Step 4: 提交**

```bash
git add -A
git commit -m "test(i18n): 构建产物端到端验证脚本"
```

---

## 遗留事项

以下项目**不在**本计划范围，记录以免遗忘：

- `server/api/stats.get.ts` 固定统计 `content_zh`。三语言下「全站字数」该怎么算是产品决策，未定。
- `nuxt-llms` 生成的 llms.txt 目前只覆盖中文。
- `app.config.ts` 的 `stats.wordCount: '约7万'` 是手填值，三语言下需要各自维护。
- 译文本身由作者补写。本计划只保证 `content/{en,ja}/` 与 `app/travels/{en,ja}/` 里放进文件后能被正确路由、索引、发订阅源与 hreflang。
